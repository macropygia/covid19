name: Language File Limitation

on:
  pull_request:
    branches:
      - action
      - development
    paths:
      - 'assets/locales/*.json'
      - '!assets/locales/ja.json'

jobs:
  lang_file_limitation:
    name: Language File Limitation
    runs-on: ubuntu-18.04
    steps:
#      - name: actor
#        env:
#          GITHUB_ACTOR: ${{ github.actor }}
#        run: echo "$GITHUB_ACTOR"
      - name: Transifex
        if: github.actor == 'transifex-integration'
        run: echo you are transifex
      - name: macropygia
        if: github.actor == 'macropygia'
        run: echo you are me!
#      - name: The others
#        if: github.actor == "transifex-integration"
#        run: echo you are someone
